# General
Inform what directories are in direct run 
`echo $PATH`

| Command | Description |
| --- | --- |
| rm -r -f myfolder | Remove dir by -force, -recursive |
| `mv mybefore myafter` | Rename folder |
| `mkdir src` | Make folder |
| `pwd` | Print current directory |
| `cd ..` | Change directory (up) |
| `echo "Hello" > example.txt` | Write to file |
|     | Client URL.  <br>Command line to send request |
| echo "Hello" > example.txt |     |
|     |     |
|     |     |
|     |     |
|     |     |
|     |     |

# CURL
`curl -o output.html https://www.example.com`
-o: Save response to file
-H: Headers

# Misc

Mouse speed read/write:
```
defaults read -g com.apple.mouse.scaling
defaults write -g com.apple.mouse.scaling  5.0
```